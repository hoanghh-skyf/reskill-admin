"use client";

import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip";

export const AppTooltipProvider = TooltipProvider;
export const AppTooltip = Tooltip;
export const AppTooltipTrigger = TooltipTrigger;
export const AppTooltipContent = TooltipContent;
